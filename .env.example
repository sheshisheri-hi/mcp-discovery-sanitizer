# No secrets. This sanitizer is CPU-only and reads JSON from disk or stdin.
# Optional CLI-equivalent knobs if you wrap it in a gateway (not read by the package today):
# MCP_SANITIZER_FAIL_CLOSED=1
# MCP_SANITIZER_ALLOW_PUBLIC_CACHE=0
# MCP_SANITIZER_MAX_INSTRUCTIONS_LEN=1024
